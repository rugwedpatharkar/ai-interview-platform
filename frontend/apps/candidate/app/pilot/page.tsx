import { PilotClient } from "./page-client";

export const metadata = {
  title: "Request a pilot — Aptura",
  description:
    "Open a hiring pilot with a small set of teams hiring on proven merit.",
};

export default function PilotPage() {
  return <PilotClient />;
}
