// Lifted to @ip/ui (byte-identical; typed against a structural NotificationItemData that the
// app's `Notification` satisfies). Re-exported so existing imports resolve unchanged.
export { NotificationItem, type NotificationItemProps } from "@ip/ui";
