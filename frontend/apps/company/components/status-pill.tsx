// The canonical status pill now lives in @ip/ui (label + tone come from the shared
// application map, with the advisory-gate overrides applied). Re-exported here so the
// existing in-app import paths keep working.
export { StatusPill, type StatusPillProps } from "@ip/ui";
