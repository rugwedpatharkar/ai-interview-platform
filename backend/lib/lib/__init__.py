"""lib — shared infrastructure for the AI interview platform.

Submodules: config, logging, mongodb, redis, rabbitmq, security, schemas.
Import what you need, e.g. `from lib.mongodb import MongoManager`.
"""
